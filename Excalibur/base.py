import numpy as np

class BaseRegressor:
    def __init__(self, verbose=False):
        self.verbose = verbose

class BaseClassifier:
    def __init__(self, verbose=False):
        self.verbose = verbose

class BaseCluster:
    def __init__(self, verbose=False):
        self.verbose = verbose

class BaseNeuralNetwork:
    def __init__(self, verbose=False):
        self.verbose = verbose